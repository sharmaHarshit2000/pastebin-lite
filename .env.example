# Upstash Redis (REST)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Deterministic time testing for automated graders / local tests.
# When TEST_MODE=1, the header x-test-now-ms is used as the current time for expiry logic.
TEST_MODE=0